#version 450

layout(local_size_x =512, local_size_y = 2, local_size_z = 1) in;

layout(set = 0, binding = 0) buffer Data {
    uvec4 data[];
} buf;

void main() {
    uint idx = gl_GlobalInvocationID.x;
		uint idy = gl_GlobalInvocationID.y;
    buf.data[idy * 512 + idx] = uvec4(5);
}